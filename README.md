## Deploy simple web app to cloud

- render.com
- huggingface.co   # need to remove README.md since HF use it for configuration???
- aws beanstalk

## run locally
- export GROQ_API_KEY = "...", then (choose one):
- python -m gunicorn app:app
- pthon app.py
